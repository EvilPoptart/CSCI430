package com.csci430.anandroidgame;

public class Graphics extends Global{
	//animation function
	//image spriteSheets[]
	
	Graphic bg;		//will be replaced by level image eventually
	
	/*
		Reference global to pull screen sizes.
		ie Global.screenWidth
	*/

}
/*
public void doDraw()
{
	draw each object if visible in the vector super.worldObjects
ex:
	canvas.save()

	bg.draw(canvas)
	for(all indexes of super.worldObjects)
	{
		if(Global.worldObjects[index].visible)
			Global.worldObjects[index].updateAndDraw(canvas)
		else
			dont draw object (ie make it go away if it existed)
	}

	canvas.restore()
	
	
}
*/